server {
    listen 80 default_server;
    listen [::]:80 default_server;

    root /var/www/elektrum_frontend/prelive;
    index index.php index.html index.htm;

    server_name default;

    location / {
        try_files $uri $uri/ =404;
    }

    location ~ \.php$ {
        include snippets/fastcgi-php.conf;
        fastcgi_pass unix:/run/php/php7.0-fpm.sock;
    }
}